#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
BACKEND="$SCRIPT_DIR/trackrec-enrich-spot"

if [[ ! -x "$BACKEND" ]]; then
  echo "Missing internal backend: $BACKEND" >&2
  exit 3
fi

exec "$BACKEND" "$@"